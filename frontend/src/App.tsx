import {AppShell} from '@/components/app-shell';
import {WorkspaceView} from '@/components/workspace-view';

function App() {
    return (
        <AppShell>
            <WorkspaceView/>
        </AppShell>
    )
}

export default App
